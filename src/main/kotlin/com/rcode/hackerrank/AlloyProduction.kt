package com.rcode.hackerrank

import kotlinx.coroutines.flow.flowOf
import kotlin.reflect.typeOf

fun findMaximumAlloyUnits() {}

fun main(args: Array<String>) {
    var x = 1
    var y = 10
    var z = 1

    var a = mutableListOf(1,2,3)

    //print(a)

    var stringInput = ",10,15,,20"
    /*stringInput
        .trim()
        .split(",").toTypedArray()
        .map { value -> value.toInt()}
        .forEachIndexed { index, i ->
            println("index ${index}, value ${i}")
        }*/

    val arr = arrayOf(1,2,3)
    /*do {

        print("code executed")
    } while ( y>1 && x<10)*/
}